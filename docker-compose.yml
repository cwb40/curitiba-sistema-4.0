version: "3.8"

services:
  api:
    image: python:3.11
    container_name: api_curitiba
    working_dir: /app
    volumes:
      - ./API:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
